#include "common.h"
namespace resolve1069 {
	int resolve();
	void change_to_str(int digits, vector<char>& digit_chr);
	void change_to_int(vector<char>& digit_chr, int& digits);
	bool cmp_asc(char c1, char c2);
	bool cmp_desc(char c1, char c2);
}