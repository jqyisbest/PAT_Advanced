#include "common.h"
namespace resolve1119 {
	int resolve();
	void get_inorder(bool &is_unique, int pre_l, int pre_r, int post_l, int post_r, vector<int> &pre_order, vector<int> &post_order, vector<int> &in_order);
}